import { HighlightMovieDirective } from './highlight-movie.directive';

describe('HighlightMovieDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightMovieDirective();
    expect(directive).toBeTruthy();
  });
});
